///tmc_vec2_len(tmc_vec2)
/*
    returns the len of a vector, asuming the vector is a scalar
    var l = tmc_vec2_len(tmc_vec2(10,10));
*/
return point_distance(0,0,
        argument0[1],
        argument0[2]
        );